$(function () {
    $('#slider').slick({
        autoplay: true, // 自動再生
        dots: true, // ドット上のナビを表示
        arrows: true, // 前/次にスライドを切り替える矢印
        fade: true,
        cssEase: 'linear'
    });
});